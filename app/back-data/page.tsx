'use client';

import { Box } from '@mui/material';
import React from 'react';

const BackData = () => {
  return <Box sx={{ bgcolor: (theme) => theme.palette.primary.light }}>content</Box>;
};

export default BackData;
