version: 1.0
frontend:
  phases:
    install:
      runtime-versions:
        nodejs: 18
    preBuild:
      commands:
        - echo --------Pre build Start----------
        - yarn install
    build:
      commands:
        - yarn build
    postBuild:
      commands:
        - echo --------Build End----------
  artifacts:
    baseDirectory: out
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
